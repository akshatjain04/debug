# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=42ac6fa2e0

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "Profile_accountCode_post_others.csv"
    fields: ["firstName", "lastName", "profilePin"]
    skipHeader: true
scenarios:
  - name: "Profile API Testing"
    flow:
      - post:
          url: "/Profile/12345?profileIdentifier=SYNC_EXTERNAL"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
      - think: 1
      - post:
          url: "/Profile/12345?profileIdentifier=SYNC_EXTERNAL"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalid_token"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 401
            - hasProperty: "errors"
      - think: 1
      - post:
          url: "/Profile/12345"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 404
            - hasProperty: "errors"
      - think: 1
      - post:
          url: "/Profile/12345?profileIdentifier=invalid_value"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 400
            - hasProperty: "errors"
