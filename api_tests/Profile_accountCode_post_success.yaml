# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=42ac6fa2e0

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "Profile_accountCode_post_success.csv"
    fields:
      - "firstName"
      - "lastName"
      - "profilePin"
    skipHeader: true

scenarios:
  - name: "Create Profile Test Scenario"
    flow:
      - post:
          url: "/Profile/{{ accountCode }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 200
            - hasProperty: "firstName"
            - hasProperty: "lastName"
            - hasProperty: "profilePin"
          log: "Posted a new profile for the account code {{ accountCode }}"
          think: 1
