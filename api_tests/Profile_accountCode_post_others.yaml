# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=42ac6fa2e0

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "./Profile_accountCode_post_others.csv"
    fields: ["firstName", "lastName", "profilePin"]
    skipHeader: true
  plugins:
    expect: {}
    
scenarios:
  - name: "API request with validation failure"
    flow:
      - log: "Sending a request with missing required fields"
      - post:
          url: "/Profile/invalidAccountCode"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                error: "Invalid request body"
      - log: "Received an error response as expected"
  
  - name: "API request with edge case"
    flow:
      - log: "Sending a request with large data"
      - post:
          url: "/Profile/validAccountCode"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 413
            - hasProperty: "error"
            - equals: 
                error: "Payload too large"
      - log: "Received an error response as expected"

  - name: "API request without authentication"
    flow:
      - log: "Sending a request without authentication"
      - post:
          url: "/Profile/validAccountCode"
          headers:
            Content-Type: "application/json"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: 
                error: "Unauthorized"
      - log: "Received an error response as expected"

  - name: "API request with invalid authentication credentials"
    flow:
      - log: "Sending a request with invalid authentication credentials"
      - post:
          url: "/Profile/validAccountCode"
          headers:
            Content-Type: "application/json"
            Authorization: "InvalidToken"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - equals: 
                error: "Unauthorized"
      - log: "Received an error response as expected"

  - name: "API request with wrong data type"
    flow:
      - log: "Sending a request with wrong data type"
      - post:
          url: "/Profile/validAccountCode"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            firstName: 12345
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: 
                error: "Invalid request body"
      - log: "Received an error response as expected"
