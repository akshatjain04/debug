# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=42ac6fa2e0

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "Profile_accountCode_post_success.csv"
    fields:
      - "accountCode"
      - "firstName"
      - "lastName"
      - "profilePin"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Create Profile"
    flow:
    - log: "Posting a new profile with valid accountCode, firstName, lastName, and profilePin"
    - post:
        url: "/Profile/{{ accountCode }}"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          Content-Type: "application/json"
        json:
          firstName: "{{ firstName }}"
          lastName: "{{ lastName }}"
          profilePin: "{{ profilePin }}"
        expect:
          - statusCode: 200
          - hasProperty: "firstName"
          - hasProperty: "lastName"
          - hasProperty: "profilePin"
    - log: "Successfully created a new profile"
    - think: 1
